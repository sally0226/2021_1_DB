import { createMuiTheme } from '@material-ui/core';

export default createMuiTheme({
	palette:{
		primary: {
			main: '#FFFFFF'
		},
		secondary: {
			main: '#000000'
		},
		text: {
			primary: '#FFFFFF',
			secondary: '#000000',
		}
	}
});
